#repo는 로컬저장소(local repository)와 원격저장소(remote repository)가 있다. 
로컬은 개인이며 원격은 개인이 다른곳에 접속하여 저장할수있거나 저장된 것을 불러올 수 있는 곳이다.

# Tracking(추적) 
-> 로컬 repo에서 원격 repo를 연결하는 행위를 말한다. 

# clone 
-> 원격 repo를 내 컴퓨터에 가져와서 로컬repo를 초긱 세팅하게하는 것이다(전체 복사)
